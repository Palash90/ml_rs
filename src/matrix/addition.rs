use super::*;
use std::ops::Add;
use std::ops::Div;
use std::ops::Mul;
use std::ops::Sub;

impl Add for Matrix {
    type Output = Self;

    fn add(self, m2: Self) -> Self {
        create()
    }
}
